#include<bits/stdc++.h>
using namespace std;
int main()
{
    //freopen("insum.txt","rt",stdin);
   // freopen("outsum.txt","wt",stdout);

    float a,b,c,d;
    float e,f,g,h;
    cin>>a>>b>>c>>d;
    e=ceil(a);
    f=ceil(b);
    g=ceil(c);
    h=ceil(d);

    cout<<e+f+g+h;

    return 0;
}
